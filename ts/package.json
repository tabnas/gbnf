{
  "name": "@tabnas/gbnf",
  "version": "0.1.5",
  "main": "dist/gbnf.js",
  "type": "commonjs",
  "types": "dist/gbnf.d.ts",
  "bin": {
    "gbnf-check": "dist/cli.js"
  },
  "description": "llama.cpp GBNF grammar compiler for the tabnas parser.",
  "homepage": "https://github.com/tabnas/gbnf",
  "keywords": [
    "tabnas",
    "gbnf",
    "llama.cpp",
    "grammar",
    "parser",
    "plugin"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/gbnf.git",
    "directory": "ts"
  },
  "scripts": {
    "build": "tsc --build src",
    "build-demo": "esbuild doc/demo-entry.js --bundle --format=iife --minify --outfile=../docs/gbnf-demo.js",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-approve": "npm stage approve",
    "repo-stage-list": "npm stage list",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "test": "node --enable-source-maps --test test/**/*.test.js",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test test/**/*.test.js",
    "watch": "tsc --build src -w"
  },
  "license": "MIT",
  "files": [
    "dist",
    "LICENSE"
  ],
  "exports": {
    ".": "./dist/gbnf.js"
  },
  "peerDependencies": {
    "@tabnas/bnf": ">=0",
    "@tabnas/parser": ">=0.5.0"
  },
  "devDependencies": {
    "@tabnas/abnf": "^0.4.2",
    "@tabnas/bnf": "*",
    "@tabnas/debug": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@types/node": "25.6.0",
    "esbuild": "^0.28.2",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
