__pycache__/
*.pyc

# Built libraries dropped next to the module — _default_lib_path() and
# _default_engine_path() look for them here, so they are a normal thing
# to have and never a thing to commit.
libtabnasgbnf.so
libtabnasgbnf.dylib
libtabnasgbnf.dll
libtabnasparser.so
libtabnasparser.dylib
libtabnasparser.dll
libtabnas.so
libtabnas.dylib
libtabnas.dll
