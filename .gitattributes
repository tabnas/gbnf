# The conformance corpus is llama.cpp's own bytes, and a GBNF rule ends
# at a newline: never let autocrlf rewrite these. `-text` (rather than
# `binary`) keeps them diffable, which matters when upstream changes one.
test/corpus/*.gbnf -text
